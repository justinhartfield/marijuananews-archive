---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { archivePageCount, articleUrl, excerpt, formatDate, getArticlesPage } from '../../lib/content';
const page = 1;
const pageArticles = getArticlesPage(page);
---
<BaseLayout title="All Articles" description="Browse all Marijuana News reporting and back issues from Richard Cowan." canonical="/archive/">
  <section class="hero">
    <div class="container">
      <p class="eyebrow">All articles</p>
      <h1>The full reporting run.</h1>
      <p class="lede">Page {page} of {archivePageCount}. This index keeps current coverage and the older MarijuanaNews.com back file dense, plain, and source led, like a working independent newspaper.</p>
    </div>
  </section>
  <section class="section">
    <div class="container article-list">
      {pageArticles.map((article) => (
        <article class="article-row">
          <div class="meta-rail">{formatDate(article.publication_date)}<br />{article.category || 'General'}</div>
          <div>
            <h2><a href={articleUrl(article)}>{article.title}</a></h2>
            <p>{excerpt(article, 220)}</p>
          </div>
        </article>
      ))}
    </div>
    <div class="container pagination">
      <span>Page {page} of {archivePageCount}</span>
      {archivePageCount > 1 && <a class="button" href="/archive/2/">Older articles</a>}
    </div>
  </section>
</BaseLayout>
