---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { archivePageCount, articleUrl, excerpt, formatDate, getArticlesPage } from '../../lib/content';
const page = 1;
const pageArticles = getArticlesPage(page);
---
<BaseLayout title="Archive" description="Browse the recovered MarijuanaNews.com public article archive." canonical="/archive/">
  <section class="hero">
    <div class="container">
      <p class="eyebrow">Archive</p>
      <h1>The recovered run.</h1>
      <p class="lede">Page {page} of {archivePageCount}. This index keeps the reading surface dense, plain, and source led, like a newspaper archive rather than an app dashboard.</p>
    </div>
  </section>
  <section class="section">
    <div class="container article-list">
      {pageArticles.map((article) => (
        <article class="article-row">
          <div class="meta-rail">{formatDate(article.publication_date)}<br />{article.category || 'General'}</div>
          <div>
            <h2><a href={articleUrl(article)}>{article.title}</a></h2>
            <p>{excerpt(article, 220)}</p>
          </div>
        </article>
      ))}
    </div>
    <div class="container pagination">
      <span>Page {page} of {archivePageCount}</span>
      {archivePageCount > 1 && <a class="button" href="/archive/2/">Older articles</a>}
    </div>
  </section>
</BaseLayout>
